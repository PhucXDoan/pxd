#include <stdio>

extern int
main(void)
{
	printf("This is an example with no usage of the Meta-Preprocessor.\n");
	printf("Examples are the primary form of documentation for using the Meta-Preprocessor.\n");
	printf("It is also a form of test suite.\n");
	printf("Run `cli.py test` to run the Meta-Preprocessor on every example (and have the output verified).\n");
	printf("If you don't want to or can't, see the `prebuilt` folder for expected output and generated files.\n");
}
